[package]
name = "test_fs"
version = "0.1.0"
edition = "2021"

[dependencies]
stem = { path = "../../../stem", default-features = false, features = ["rt"] }
abi = { path = "../../../abi" }

[[bin]]
name = "test_fs"
path = "src/main.rs"
test = false
doctest = false
