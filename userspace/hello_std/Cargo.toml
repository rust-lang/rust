[package]
name = "hello_std"
version = "0.1.0"
edition = "2021"

[dependencies]
stem = { path = "../../stem", default-features = false, features = ["rt"] }
abi = { path = "../../abi" }

[[bin]]
name = "hello_std"
path = "src/main.rs"
test = false
doctest = false
