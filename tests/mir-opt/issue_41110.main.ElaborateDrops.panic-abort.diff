- // MIR for `main` before ElaborateDrops
+ // MIR for `main` after ElaborateDrops
  
  fn main() -> () {
      let mut _0: ();
      let _1: ();
      let mut _2: S;
      let mut _3: S;
      let mut _4: S;
      scope 1 {
          debug x => _1;
      }
  
      bb0: {
          StorageLive(_1);
          StorageLive(_2);
          _2 = S;
          StorageLive(_3);
          StorageLive(_4);
          _4 = S;
          _3 = S::id(move _4) -> [return: bb1, unwind: bb3];
      }
  
      bb1: {
          StorageDead(_4);
          _1 = S::other(move _2, move _3) -> [return: bb2, unwind: bb4];
      }
  
      bb2: {
          StorageDead(_3);
          StorageDead(_2);
          _0 = const ();
          StorageDead(_1);
          return;
      }
  
      bb3 (cleanup): {
          drop(_2) -> [return: bb4, unwind terminate(cleanup)];
      }
  
      bb4 (cleanup): {
          resume;
      }
  }
  
