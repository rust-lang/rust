- // MIR for `aggregate_operands` before TailCopyToMove
+ // MIR for `aggregate_operands` after TailCopyToMove
  
  fn aggregate_operands(_1: u32, _2: u32) -> (u32, u32) {
      let mut _0: (u32, u32);
  
      bb0: {
-         _0 = (copy _1, copy _2);
+         _0 = (move _1, move _2);
          return;
      }
  }
  
