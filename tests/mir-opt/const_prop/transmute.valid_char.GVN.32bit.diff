- // MIR for `valid_char` before GVN
+ // MIR for `valid_char` after GVN
  
  fn valid_char() -> char {
      let mut _0: char;
  
      bb0: {
-         _0 = const 82_u32 as char (Transmute);
+         _0 = const 'R';
          return;
      }
  }
  
