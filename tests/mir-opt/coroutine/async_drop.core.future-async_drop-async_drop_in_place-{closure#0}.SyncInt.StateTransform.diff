- // MIR for `std::future::async_drop_in_place::{closure#0}` before StateTransform
+ // MIR for `std::future::async_drop_in_place::{closure#0}` after StateTransform
  
- fn async_drop_in_place::{closure#0}(_1: {static coroutine body of async_drop_in_place<SyncInt>()}, _2: std::future::ResumeTy) -> ()
- yields ()
-  {
-     let mut _0: ();
+ fn async_drop_in_place::{closure#0}(_1: Pin<&mut {static coroutine body of async_drop_in_place<SyncInt>()}>, _2: std::future::ResumeTy) -> CoroutineState<(), ()> {
+     coroutine layout {
+         variant_fields = {
+             Unresumed(0): [],
+             Returned (1): [],
+             Panicked (2): [],
+         }
+         storage_conflicts = BitMatrix(0x0) {}
+     }
+     let mut _0: std::ops::CoroutineState<(), ()>;
      let mut _3: &mut SyncInt;
+     let mut _4: ();
+     let mut _5: u32;
+     let mut _6: &mut {static coroutine body of std::future::async_drop_in_place<SyncInt>()};
  
      bb0: {
-         _3 = no_retag copy (_1.0: &mut SyncInt);
-         drop((*_3)) -> [return: bb1, unwind continue];
+         _6 = copy (_1.0: &mut {static coroutine body of std::future::async_drop_in_place<SyncInt>()});
+         _5 = discriminant((*_6));
+         switchInt(move _5) -> [0: bb6, 1: bb4, 2: bb3, otherwise: bb5];
      }
  
      bb1: {
+         _0 = CoroutineState::<(), ()>::Complete(move _4);
+         discriminant((*_6)) = 1;
          return;
+     }
+ 
+     bb2 (cleanup): {
+         discriminant((*_6)) = 2;
+         resume;
+     }
+ 
+     bb3: {
+         assert(const false, "`async fn` resumed after panicking") -> [success: bb3, unwind continue];
+     }
+ 
+     bb4: {
+         _0 = CoroutineState::<(), ()>::Complete(const ());
+         return;
+     }
+ 
+     bb5: {
+         unreachable;
+     }
+ 
+     bb6: {
+         _3 = no_retag copy ((*_6).0: &mut SyncInt);
+         drop((*_3)) -> [return: bb1, unwind: bb2];
      }
  }
  
