- // MIR for `direct` before TailCopyToMove
+ // MIR for `direct` after TailCopyToMove
  
  fn direct(_1: u32) -> u32 {
      debug x => _1;
      let mut _0: u32;
  
      bb0: {
-         _0 = copy _1;
+         _0 = move _1;
          return;
      }
  }
  
