- // MIR for `projected` before TailCopyToMove
+ // MIR for `projected` after TailCopyToMove
  
  fn projected(_1: Pair) -> u32 {
      debug pair => _1;
      let mut _0: u32;
  
      bb0: {
-         _0 = copy (_1.0: u32);
+         _0 = move (_1.0: u32);
          return;
      }
  }
  
