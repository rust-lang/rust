- // MIR for `projected_dest` before TailCopyToMove
+ // MIR for `projected_dest` after TailCopyToMove
  
  fn projected_dest(_1: u32, _2: u32) -> (u32, u32) {
      let mut _0: (u32, u32);
  
      bb0: {
-         (_0.0: u32) = copy _1;
-         (_0.1: u32) = copy _2;
+         (_0.0: u32) = move _1;
+         (_0.1: u32) = move _2;
          return;
      }
  }
  
