disallowed-types = [
    { path = "std::rc::Rc" }
]

[disallowed-types-profiles.forward_pass]
paths = [
    { path = "std::cell::RefCell", reason = "Prefer shared references" }
]

[disallowed-types-profiles.export]
paths = [
    { path = "std::sync::Mutex" }
]
