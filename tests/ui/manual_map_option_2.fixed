// run-rustfix

#![warn(clippy::manual_map)]

fn main() {
    let _ = Some(0).map(|x| {
            let y = (String::new(), String::new());
            (x, y.0)
        });

    let s = Some(String::new());
    let _ = match &s {
        Some(x) => Some((x.clone(), s)),
        None => None,
    };
}
