[package]
name = "virtio_gpu"
version = "0.1.0"
edition = "2024"

[lib]
name = "virtio_gpu"
path = "src/lib.rs"

[[bin]]
name = "virtio_gpu"
path = "src/main.rs"

[dependencies]
stem = { path = "../../stem", features = ["rt"] }
abi = { path = "../../abi" }
stem-macros = { path = "../../stem-macros" }
