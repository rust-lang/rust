[package]
name = "proto_driver_wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# No dependencies for a raw syscall driver, to keep it minimal.
# In a real driver, we might depend on a helper crate.
