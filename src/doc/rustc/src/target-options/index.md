# Target Options

All of these options are passed to `rustc` via the `-T` flag, short for "target." You can see
a version of this list for your exact compiler by running `rustc -T help`. Target options must be
set to the same value across all crates in the dependency graph.
