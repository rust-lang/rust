[package]
name = "mdbook-rustc"
version = "0.0.0"
edition = "2024"
workspace = "../rustbook"

[dependencies]
mdbook-preprocessor = "0.5.1"
serde = { version = "1.0.228", features = ["serde_derive"] }
serde_json = "1"
