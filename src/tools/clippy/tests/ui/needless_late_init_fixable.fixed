// run-rustfix

#![allow(unused, clippy::assign_op_pattern)]

fn main() {
    
    let a = "zero";

    
    
    let b = 1;
    let c = 2;

    
    let d: usize = 1;

    
    let e = format!("{}", d);
}
