image: ubuntu:latest

tasks:
  - before: echo "..."
    init: |
      cargo install --locked rustup-toolchain-install-master
      ./miri toolchain
      ./miri build
    command: echo "Run tests with ./miri test"
