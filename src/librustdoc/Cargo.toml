[package]
authors = ["The Rust Project Developers"]
name = "rustdoc"
version = "0.0.0"

[lib]
name = "rustdoc"
path = "lib.rs"
# SNAP/stage0(cargo)
doctest = false

[dependencies]
pulldown-cmark = { version = "0.1.0", default-features = false }
tempdir = "0.3"
