[package]
name = "build_helper"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = "1"
serde_derive = "1"

[features]
default = ["ci", "drop_bomb", "fs", "git", "metrics", "stage0_parser", "util"]
ci = []
drop_bomb = []
fs = []
git = []
metrics = []
stage0_parser = []
util = []
