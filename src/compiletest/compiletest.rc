#[crate_type = "bin"];

#[no_core];

#[allow(vecs_implicitly_copyable)];

use core(vers = "0.4");
use std(vers = "0.4");

import core::*;

mod procsrv;
mod util;
mod header;
mod runtest;
mod common;
mod errors;

// Local Variables:
// fill-column: 78;
// indent-tabs-mode: nil
// c-basic-offset: 4
// buffer-file-coding-system: utf-8-unix
// End:
