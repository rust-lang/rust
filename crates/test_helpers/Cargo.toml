[package]
name = "test_helpers"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
proptest = { version = "1.0", default-features = false, features = ["alloc", "std"] }
float-cmp = "0.10"
