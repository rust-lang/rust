#### Note: this error code is no longer emitted by the compiler.

The compiler used this error for invalid `rustc_on_unimplemented` filters. It
now emits the `malformed_diagnostic_filters` lint instead.
